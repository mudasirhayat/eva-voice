from pathlib import Path

try:
    AUDIO_DIR = Path("wav")
    audio_file = str(AUDIO_DIR / "crab-story" / "mono_1.wav")
except Exception as e:
    print(f"An error
    story_text = "OK fresh start, how about this... Close your eyes for a second"
    audio_file_2 = str
try:
    str(AUDIO_DIR / "crab-story" / "mono_3.wav"): "If pip's bouncing off the shell walls with excitement, he probably wouldnt wait for a map or hidden tunnels.  hehe
}
